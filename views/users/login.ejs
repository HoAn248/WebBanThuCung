<!DOCTYPE html>
<html lang="en">
<% include ../partials/header %>


  <body class="text-light">
    <video src="/images/video.mp4" muted loop autoplay>
    </video>
    <div class="container">
      <% include ../partials/toppage%>
        <div class="row d-flex justify-content-center">
          <div class="col-5">
            <form action="/users/login" method="post">
              <div class="row d-flex justify-content-center">
                <h1 class="">Login</h1>
              </div>
              <div class="form-group">
                <label for="inputEmail">Email</label>
                <input type="text" name="email" class="form-control" id="inputEmail" placeholder="Nhập email">
                <small class="form-text text-muted">Email này đã tồn tại.</small>
              </div>
              <div class="form-group">
                <label for="inputPassword">Password</label>
                <input type="password" name="password" class="form-control" id="inputPassword" placeholder="Nhập mật khẩu">
                <small class="form-text text-muted">Email này đã tồn tại.</small>
              </div>
              <button type="submit" class="btn btn-primary btn-lg btn-block">Đăng nhập</button>
              <a href="/users/regis" class="btn btn-primary btn-lg btn-block">Đến đăng kí :3</a>
              <a href="/" class="btn btn-primary btn-lg btn-block">Quay zìa!</a>
              <p style="color: red;">
                <%= thongBao %>
              </p>
            </form>
          </div>
        </div>
    </div>
    <% include ../partials/footer%>
  </body>
  <style>
    small {
      display: none !important;
    }
    video{
      left: 50%;
      transform: translateX(-50%);
      position: fixed;
      width: 100%;
      height: 90vh;
      z-index: -5;
    }
  </style>

</html>